<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- 短信取件码自动识别（可选开关；RECEIVE_SMS 用于接收 SMS_RECEIVED 广播） -->
    <uses-permission android:name="android.permission.RECEIVE_SMS" />

    <!-- 声明短信能力为可选硬件：避免无 telephony 硬件（如平板/Wi-Fi-only）设备被 Play 过滤，lint PermissionImpliesUnsupportedHardware 要求 -->
    <uses-feature
        android:name="android.hardware.telephony"
        android:required="false" />

    <application
        android:name=".App"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="码上闪记"
        android:supportsRtl="true"
        android:theme="@style/Theme.PickupCode">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:configChanges="orientation|screenSize"
            android:theme="@style/Theme.PickupCode">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Share from other apps (system share sheet, OEM drag-and-drop) -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="image/*" />
            </intent-filter>

            <!-- 多图分享（ACTION_SEND_MULTIPLE）：进入后给提示，避免静默丢弃无感知 -->
            <intent-filter>
                <action android:name="android.intent.action.SEND_MULTIPLE" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="image/*" />
            </intent-filter>

            <!-- Text selection menu (standard Android long-press floating toolbar) -->
            <intent-filter>
                <action android:name="android.intent.action.PROCESS_TEXT" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>

            <!-- Broad text/* for better OEM matching -->
            <intent-filter>
                <action android:name="android.intent.action.PROCESS_TEXT" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/*" />
            </intent-filter>
        </activity>

        <!-- Accessibility service: node text + screenshot OCR -->
        <service
            android:name=".service.PickupCodeAccessibilityService"
            android:exported="true"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <receiver
            android:name=".notification.NotificationDismissReceiver"
            android:exported="false" />

        <receiver
            android:name=".notification.DoneReceiver"
            android:exported="false" />

        <receiver
            android:name=".notification.RemindReceiver"
            android:exported="false"
            android:directBootAware="true" />

        <!-- Quick Settings tile -->
        <service
            android:name=".service.PickupCodeTileService"
            android:exported="true"
            android:icon="@drawable/ic_tile_pickup"
            android:label="码上闪记"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <!-- 双音量键快捷触发（系统无障碍快捷方式通道；触发即退出，不常驻） -->
        <service
            android:name=".service.VolumeKeyShortcutService"
            android:exported="true"
            android:label="码上闪记（音量键快捷方式）"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/volume_key_shortcut_config" />
        </service>

        <!-- 短信取件码自动识别（SMS_RECEIVED 广播；需 RECEIVE_SMS 权限） -->
        <receiver
            android:name=".service.SmsReceiver"
            android:exported="true"
            android:permission="android.permission.BROADCAST_SMS">
            <intent-filter>
                <action android:name="android.provider.Telephony.SMS_RECEIVED" />
            </intent-filter>
        </receiver>

        <!-- C4: 成绩卡图片分享 FileProvider -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>
</manifest>
