# v1.0.8 更新报告

versionCode 23 · 2026-08-12

---

## 概述

v1.0.8 是一次以**识别准确率**和**数据可靠性**为核心的迭代，包含 1 个重构 PR 合并、全量审查 27 项修复、4 轮定向迭代，总计 16 个提交。

---

## 识别准确率

### 多通知同屏地址串台修复
S0（取件地址标签）的折叠地址补全原来在**全屏所有行**里搜索，多条通知同屏时会抓到别人的地址。现在限定在标签行所在卡片窗口内（±3 行）。

### 短信路径补置信度过滤
短信路径之前不检查 `confidenceThreshold`，弱候选也会入库。现在与其他两条路径一致，低于阈值的候选被过滤。

### 短信路径改用逐码窗口取地址
短信原来用全屏 11 步级联，现在改为逐码窗口模式，与无障碍/分享路径对齐。

### AI 识别 Markdown 清理增强
原来只清理 ```json 和 ``` 两种字面量，现在用正则匹配所有语言标签的围栏（JSON / typescript 等变体），减少因格式解析失败而丢弃的正确结果。

### 用户纠错回喂已学规则
用户标记"不是取件码"后，现在会给匹配到该码的已学规则累计 badCount。单条规则被标记 ≥3 次后自动停用，不再永远生效。

---

## 数据可靠性

### DAO 补全逻辑修复
`insertCheckDuplicate` 原来会先补全旧行再插入新的空行——首页取 `MAX(id)` 展示的正是那条空行。现在补全后直接复用旧行。

### Kuaidi100 回填定向 UPDATE
快递 100 反查回填原来用整行 `update`，在异步回填期间可能覆盖用户中间操作（如已取归档）。现在改为定向只更新地址列。

### 截图文件跟随清理
回收站 24 小时删除原来只清 DB 行，截图文件从不清理。现在删除过期行前先清理对应文件。

### 稍后提醒自动取消
用户设了"稍后提醒"后提前取件，原来的闹钟不会被取消。现在"已取"操作同时取消对应的 AlarmManager 定时器。

---

## 代码质量

### 全量审查修复（27 项）
- HIGH 3 项：`dedupeRepeated` 多字符 CJK 正则修复、AI 原始响应日志、AI 错误体异常加 Log
- MEDIUM 13 项：CancellationException 漏吞、`addExclude` 加 @Synchronized、DB 补迁移、循环内 Regex 提常量等
- LOW 11 项：死代码、`!!` 消除、close() try-finally、Compose `remember` 等

### 处理流程重构（PR #13）
- AddressExtractor / BrandResolver / CodeValidator 从 CodeExtractor 拆出
- `extractLocation` 拆为 14 步骤 + `LocationState`
- `tryExtract` 拆为 9 步骤
- 30 个评分/几何魔数命名常量

### 回归测试落地
19 个 JUnit 5 单元测试：`dedupeRepeated` 多字符回归、S0/S5/S6/S10 地址提取、格式白名单边界、格式 ID 稳定性。

---

## 文件变更

| 轮次 | 文件数 |
|------|--------|
| PR #13 合并 + 重构 | 11 |
| 全量审查修复 | 13 |
| S0/DAO/纠错 | 7 |
| 短信路径对齐 | 1 |
| 单元测试 | 3 |
| 6 条定向修复 | 7 |

---

## 已知限制

- `extractAddressForCode` ±3 行窗口在 1080p 屏上可能跨卡
- PatternLearner 是启发式聚类，非真机器学习
- 无 ViewModel/Repository 分层，UI 直连 DAO
